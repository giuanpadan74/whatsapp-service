[variables]
NIXPACKS_NODE_VERSION = "22.13.0"
NPM_CONFIG_PRODUCTION = "false"

[phases.setup]
nixPkgs = ["...", "curl", "wget"]
aptPkgs = ["...", "curl", "wget"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.js"
